# No API key is required for the current prototype.
# The analytical engine is deterministic and runs locally in the client.

# Optional: disable Vite HMR in constrained preview environments.
DISABLE_HMR=false
